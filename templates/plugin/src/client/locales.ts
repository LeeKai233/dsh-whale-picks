/** Locale copy for the {{PLUGIN_ID}} row. */
export interface ExampleKey {
  rowLabel: string
}

export const zh: ExampleKey = {
  rowLabel: '示例开关（{{PLUGIN_ID}}）',
}

export const en: ExampleKey = {
  rowLabel: 'Example toggle ({{PLUGIN_ID}})',
}
